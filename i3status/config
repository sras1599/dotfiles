general {
  colors = true
}

order += "wireless _first_"
order += "battery 0"
order += "volume master"
order += "time"

wireless _first_ {
  format_up = "%essid %ip"
  format_down = "W: down"
}

battery 0 {
  format = "%status %percentage %remaining"
  last_full_capacity = true
}

time {
  format = "%d-%m-%Y %H:%M"
}

volume master {
  format = "♪: %volume"
  device = "default"
  mixer = "Master"
  mixer_idx = 0
}
